# ── Image tag (set by CI deploy job) ───────────────────────
IMAGE_TAG=mcp-metro:latest

# ── Host port (external port on the server) ────────────────
HOST_PORT=9049

# ── Service identity ──────────────────────────────────────
SERVICE_NAME=mcp-metro
SERVICE_INSTANCE=prod
NODE_ENV=production
